%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% fun1.m, a function that corresponds to f(x) = 1 + 0.5*sin(x) - x, 
% taking x as input and returning output f(x). 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

function fx = fun1(x) %Declaring output fx, input x.

fx = 1 + 0.5*sin(x) - x; %Actual algebra.

end