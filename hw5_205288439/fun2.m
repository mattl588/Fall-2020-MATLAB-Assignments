%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% fun2.m, a function that corresponds to g(x) = 1 + 0.5*sin(x), 
% taking x as input and returning output g(x). 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
function gx = fun2(x) %Declaring function; output gx, x input.

gx = 1 + 0.5*sin(x); %Actual calculation.

end